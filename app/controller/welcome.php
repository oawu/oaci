<?php defined ('BASEPATH') || exit ('此檔案不允許讀取。');

/**
 * @author      OA Wu <comdan66@gmail.com>
 * @copyright   Copyright (c) 2013 - 2017, OACI
 * @license     http://opensource.org/licenses/MIT  MIT License
 * @link        https://www.ioa.tw/
 */

class welcome extends Controller {

  public function index () {
    // Load::sysLib ('MigrationTool.php', '載入 MigrationTool 失敗！');
    // MigrationTool::init ();
    // MigrationTool::to (0);
    // MigrationTool::create ('x');
    
    // $a = Cache::file ('ww', function () {
    //     return 'ss';
    // }, 10);

    // echo '<meta http-equiv="Content-type" content="text/html; charset=utf-8" /><pre>';
    // var_dump ($a);
    // exit ();

    // Load::sysLib ('Session.php');
    // Session::init ();

    // use_model ();

    // Load::sysLib ('CacheDrivers/Memcached.php');
    // $cache = new CacheMemcachedDriver ();
    // // $cache->setAdapter ('file');
    // // $cache->save ('aaa', 1);
    // echo '<meta http-equiv="Content-type" content="text/html; charset=utf-8" /><pre>';
    // // var_dump ($cache->isSupported ());
    // var_dump ($cache->save ('a', '1111111', 1000));
    // // var_dump ($cache->isSupported (), $cache->save ('a', array (1,2,3)), $cache->get ('a'));
    // // var_dump ($cache->increment ('a', 4));
    // var_dump ($cache->get ('a'));
    // var_dump ($cache->clean ());
    // // var_dump ($cache->metadata ('a'));
    // exit ();;


    // // $a = Article::find ('first');
    
    // $test = new UnitTest ();
    // $test->run ($a, array (), 'aaa');
    // echo $test->report ();


    // $name = '/mydata1.txt';
    // $data = 'A Data String!';

    // $zip = new Zip ();
    // // $zip->addDir ('ss');//->addFile (FCPATH . 'log/log-2017-12-04.log', 'a/ds.log');
    // $zip->addDir (FCPATH . 'log/', true);
    // // ->addFile (FCPATH . 'log/log-2017-12-04.log', 'a/ds.log');

    // // 將 zip 檔案寫入到伺服器的目錄內，命名為 "my_backup.zip"
    // $zip->save (FCPATH . 'log/my_backup.zip');


    // // $zip->addDir (FCPATH . 'log/');
    // $zip->addData (FCPATH . 'log/log-2017-12-04.log', 'asd');
    // $zip->archive (FCPATH . 'a.zip');

    // echo '<meta http-equiv="Content-type" content="text/html; charset=utf-8" /><pre>';
    // var_dump (extension_loaded ('mbstring') && ini_get('mbstring.func_overload'));
    // exit ();
    // UserAgent::init ();

    // echo '<meta http-equiv="Content-type" content="text/html; charset=utf-8" /><pre>';
    // var_dump (UserAgent::getRobot ());


// echo '<meta http-equiv="Content-type" content="text/html; charset=utf-8" /><pre>';
// var_dump ('~',   Session::setData ('v', 'aswwwwwwd'), Session::getData ('v'));
// exit ();
    // $view = View::create ('welcome_message.php')
    //             ->with ('a', '123')
    //             ->get ();

    // return Output::view ($view);
  }
}
